---
title: Dot.Auction — Platform Wiki
version: 0.8.0
status: active
owners: [Auction Platform Lead]
platform-id: dot-auction
last-review: 2026-08-06
---

# Dot.Auction

Purpose: this is Dot.Auction's own knowledge home — owned and maintained by the Dot.Auction team. It describes what this platform is, what it stores, and how it connects to the wider Dot Ecosystem. Dot.Brain never edits this file; it only reads what we choose to publish.

> **Related:** [Dot.Brain's ingested view of this platform](https://github.com/sakhilebhayi/Dot.Brain/blob/main/platforms/dot-auction.md)

---

## 1. What Dot.Auction Is

Dot.Auction is the live-bidding platform of the Dot Ecosystem: sellers list items with a starting price and a fixed auction window, buyers bid in real time, and the platform tracks the current highest bid, enforces reserve prices, and settles the winner the moment the clock runs out. The tagline in our own README says it plainly — *"List items, place live bids, and win with confidence."*

**Status:** built and running as a Laravel application, not a blueprint. The repository has a working schema (`auctions`, `bids`, `auction_categories`, `watchlists`, `auction_items`), Eloquent models with real relationships and business logic, a broadcast event that pushes bids to listeners over WebSockets, and an authenticated seller dashboard. What is not yet built: buyer-facing bidding UI beyond the dashboard, auto-bid execution logic, dispute resolution, and the Knowledge Pack publishing pipeline described in §6. Treat the architecture and domain sections below as current shipped behavior; treat the roadmap in §7 as what comes next.

## 2. Architecture

| Layer | Technology |
|---|---|
| Framework | Laravel 12 (PHP 8.4) |
| Frontend | Livewire 3 · Alpine.js 3 · Tailwind CSS |
| Auth / accounts | Laravel Jetstream + Sanctum, team-based accounts |
| Realtime | Laravel Reverb (WebSocket bid broadcasting) |
| Database | PostgreSQL 16 — the shared ecosystem instance (`DB_DATABASE=infodot`) |
| Queue | Database-backed queue (Redis available for scale-out) |
| Search | Laravel Scout · Meilisearch (planned integration point) |
| AI | Anthropic Claude (`claude-sonnet-4-6`) via `ANTHROPIC_API_KEY` |

Dot.Auction runs on the ecosystem's shared PostgreSQL instance rather than an isolated database, and authenticates through the ecosystem SSO handoff (`EcosystemAuthController` at `/auth/ecosystem`, Sanctum tokens) rather than standing up its own identity system. This is a deliberate ecosystem-integration choice, not an artifact of early scaffolding — Dot.Auction is one of the platforms sharing the InfoDot/Dot data plane by design.

Real-time bidding is the architectural centerpiece: when a bid is placed, `App\Events\BidPlaced` broadcasts on a per-auction channel (`auction.{auction_id}`) carrying the new amount, bidder name, and updated current price, so every connected watcher sees the price move without polling.

## 3. Domain Entities

| Entity | Table | Key fields | Notes |
|---|---|---|---|
| Auction | `auctions` | `seller_id`, `category_id`, `starting_price`, `reserve_price`, `current_price`, `buy_now_price`, `bid_increment`, `status`, `starts_at`/`ends_at` | Status lifecycle: `draft` → `active` → `ended`/`cancelled`. `isActive()` checks status *and* the current time window. `minimumBid()` derives the next legal bid from `current_price + bid_increment`. |
| Bid | `bids` | `auction_id`, `bidder_id`, `amount`, `is_winning`, `is_auto_bid` | One row per bid attempt; `is_winning` flags the current leader per auction. |
| AuctionCategory | `auction_categories` | `name`, `slug` | Simple taxonomy; auctions optionally belong to one. |
| Watchlist | `watchlists` | `user_id` + `auction_id` composite key | Buyer-side interest tracking, no extra payload yet. |
| AuctionItem | `auction_items` | `auction_id`, `name`, `condition`, `location` | Item-level detail attached to an auction (condition, physical location) — supports auctions that bundle multiple physical items under one listing. |

Relationships worth noting: `Auction::winningBid()` is a dedicated `HasOne` scoped to `is_winning = true`, kept separate from the full `bids()` history (`HasMany`, latest-first) so the UI can cheaply show "current leader" without scanning bid history.

## 4. Events Emitted

| Event | Trigger | Channel / consumers | Payload |
|---|---|---|---|
| `BidPlaced` (broadcast) | A bid is recorded on an active auction | Reverb channel `auction.{auction_id}` — anyone watching that auction's live view | `auction_id`, `amount`, `bidder` (name), `current_price` |

Only one domain event is wired today. The natural next events — auction closed/settled, reserve not met, watchlist notification, ecosystem-facing settlement handoff to Dot.Billing — are not yet implemented; see the roadmap.

## 5. Connecting to Dot.Brain

Dot.Auction participates in the ecosystem as a registered platform (`dot-auction`). Dot.Brain's ingested view of this platform — including the mechanism-scoping rules that govern what Auction is allowed to publish — lives at [`platforms/dot-auction.md`](https://github.com/sakhilebhayi/Dot.Brain/blob/main/platforms/dot-auction.md) in Dot.Brain. That document is authoritative for integration mechanics (Knowledge Pack schedule, entity mapping, manifest, worked round-trips); this wiki is authoritative for what Dot.Auction actually *is* and what's actually built.

The governing principle carried over from Dot.Brain's view, and honored here even though the publishing pipeline itself doesn't exist yet: **individual bids and bidder identity never leave this platform.** What Dot.Auction intends to publish, once the Knowledge Pack pipeline is built, is clearing-level and mechanism-level knowledge only:

| Payload type | Would contain |
|---|---|
| `observation` | Clearing-rate and price-vs-estimate aggregates, batched per settled auction, never per bid |
| `insight` | Mechanism-fit findings (which auction format clears which category best) |
| `outcome` | Verification of prior Dot.Brain recommendations (e.g., did switching a category to a different format move `auction.clearing_rate`) |
| `incident` | Mechanism failures or scoping-gate near-misses |

Nothing about an auction should publish until it settles (status reaches `ended`), and bid-level data should never be aggregated below a safe cohort size. These rules exist in Dot.Brain's document today; implementing them in code is open work here.

## 6. Roadmap

- [x] Buyer-facing live bidding UI (platform-loop pass, 2026-08-01): `/auctions` marketplace and `/auctions/{auction}` detail page now render `App\Livewire\Auctions\BidPanel`, which previously had no Blade view at all. Search/filter by title, category, status. Watchlist toggle and an outbid notification (real, live-triggered — not scheduled) were added alongside it. See §8 Change Log for the full list.
- [ ] Auto-bid execution (the `is_auto_bid` column exists on `bids`; no engine acts on it yet)
- [ ] Auction settlement job: on `ends_at` passing, resolve winner, flip `status` to `ended`, and emit a settlement event
- [ ] `auction.reserve.not_met` event when a lot ends below reserve
- [ ] Dispute resolution workflow (mentioned in the platform README, not yet modeled)
- [ ] Knowledge Pack publisher: settlement-triggered `observation` packs to Dot.Brain, respecting the never-publish-individual-bids rule
- [ ] Search integration (Scout + Meilisearch) for auction discovery
- [ ] Ecosystem settlement handoff to Dot.Billing on auction close

## Change Log

| Version | Date | Author | Change |
|---|---|---|---|
| 0.8.0 | 2026-08-06 | Platform-loop pass | Ecosystem standardization pass (rolled out from InfoDot's pilot, `a69e47a`): branded email theme, complete legal pages, and the welcome hero's photo carried onto the auth pages. **Legal pages**: `terms.blade.php`/`policy.blade.php` were already wired to Jetstream's `{!! $terms !!}`/`{!! $policy !!}` markdown convention but `Features::termsAndPrivacyPolicy()` was commented out and the markdown source was still Jetstream's 3-line stock placeholder — enabled the feature and wrote real POPIA-aligned Privacy Policy and Terms & Conditions into `resources/markdown/{terms,policy}.md`, naming BluePin Inc as the responsible party, grounded in this platform's real domain model (listings, reserve prices, live bids, auction settlement — per §1's own description of what's actually built). Added a Cookie Policy the same way (no Jetstream equivalent), grounded in the actual `dot-auction-session`/`XSRF-TOKEN`/`remember_web_*` cookies this app sets. Added Privacy/Cookies/Terms links to the welcome footer, which had none since the routes didn't exist before. **Auth hero**: `components/authentication-card.blade.php` had a flat `--ink` background with no photo — added the welcome page's own hero photo (wooden gavel, Sasun Bughdaryan) as a full-bleed backdrop with a radial + linear scrim graduated for the centered-card layout, matching the treatment InfoDot's pilot established. `terms.blade.php`/`policy.blade.php`/`cookies.blade.php` build their own wrapper directly rather than going through this component, so they correctly stay on the flat ink background — no change there, consistent with keeping prose-heavy pages off a busy photo backdrop. **Email theme**: published Laravel's Markdown mail theme and branded it to this platform's ink/gold/red/paper tokens; dark `#17110f` header band with the real logo, gold pill buttons with dark text. All hardcoded hex, no CSS variables. Verified by rendering a real `ResetPassword` notification through a temporary local preview route (removed before shipping). Verified: `npm run build` clean, `php artisan test` 60/60 passing (7 skipped, pre-existing), zero console errors, checked at 1280px and 375px. |
| 0.7.1 | 2026-08-06 | Platform-loop pass | Guest-facing design pass, auth-page half only — the hero-photo task was skipped intentionally (`welcome.blade.php` already has the real Sasun Bughdaryan gavel photo from 0.4.0/0.7.0; that file was not opened or touched in this pass). Confirmed the seven Jetstream stock auth views (`resources/views/auth/{login,register,forgot-password,reset-password,verify-email,confirm-password,two-factor-challenge}.blade.php`) were still the unmodified scaffold and restyled at the component layer so it cascades to all of them: `layouts/guest.blade.php` now imports the same Fraunces/Work Sans/IBM Plex Mono Google Fonts stack and `:root` ink/paper/gold/red custom properties as `welcome.blade.php`, and removes the pre-existing `localStorage`/`prefers-color-scheme` dark-mode script and the fixed moon/sun toggle button from the guest scope only (`layouts/app.blade.php`, the authenticated dashboard layout, was not touched and keeps its own dark-mode code) — guest pages now render on the single fixed ink/paper theme, matching `welcome.blade.php`. `components/authentication-card.blade.php` swapped its `bg-gray-100`/`bg-white`/`dark:` variants for `bg-[var(--ink)]` outer and `bg-[var(--ink-soft)]` card with a `border-[var(--line)]`. `components/authentication-card-logo.blade.php` changed `size-16` (no responsive scaling) to `h-16 sm:h-20 w-auto`, matching the nav logo convention already proven on `welcome.blade.php`. `components/{input,label,input-error,validation-errors,button,checkbox}.blade.php` recolored borders/focus rings/backgrounds to the brand tokens (gold focus rings, red-soft error text, gold submit buttons in `font-display`) and dropped every `dark:`/`indigo-` class, since the guest scope no longer toggles a `dark` class on `<html>`. Spot-checked and hand-edited the remaining gray/indigo/`dark:` text in each of the 7 auth pages individually (status messages, "Forgot your password?"/"Already registered?"/terms-and-privacy links, the 2FA recovery-code toggle buttons) — confirmed via `grep -rn "dark:\|gray-\|indigo-"` across the whole auth/guest/component set that none remained. `resources/views/terms.blade.php` and `policy.blade.php` (real standalone guest pages, not wired through `x-authentication-card`) got the same `bg-[var(--ink)]`/`bg-[var(--ink-soft)]` treatment plus `prose-invert` applied unconditionally instead of `dark:prose-invert` (dead code now that the `dark` class is never added) with `prose-headings:font-display`/`prose-a:text-[var(--gold-soft)]` for brand-consistent long-form text. Verification: `npm run build` clean. Served locally via `php artisan serve --port=8803` with `SESSION_DRIVER=array CACHE_STORE=array QUEUE_CONNECTION=sync` overridden for this one-off preview only (`.env` untouched, still pointed at the not-present-in-sandbox `dot_auction_pilot` database). `/login`, `/register`, `/forgot-password`, `/reset-password/{token}` (invalid token, form still renders) all rendered correctly with no console errors and no horizontal overflow at 375px or 1600px. `/confirm-password` 404s and `/two-factor-challenge` 302-redirects unauthenticated, as expected for session/auth-gated routes — not forced through a full login flow. `terms.md`/`policy.md` render correctly under `/terms-of-service`/`/privacy-policy` too: confirmed by temporarily uncommenting `Features::termsAndPrivacyPolicy()` in `config/jetstream.php` (normally disabled in this app) to exercise the real routes and the register page's terms-checkbox links, then reverting that config line before commit — `git diff config/jetstream.php` is empty in the final state. |
| 0.7.0 | 2026-08-06 | Platform-loop pass | Redesigned `resources/views/welcome.blade.php`, following the ecosystem's guest-facing-design pattern piloted on `mines`/Dot.Mines (commits `d191d10`, `dfc4547`) but grounded entirely in Dot.Auction's own real subject matter, not Mines' palette or type pairing. The prior page was the same generic dark-SaaS template the pilot was written to warn against: near-black background, amber gradient-text headlines, floating blurred glow orbs, a `float` keyframe animation, and a 3-equal-card icon-in-colored-box feature grid. Opened and visually read the real `public/images/logo.png` (a gold-circle gavel mark, a red chevron, and a `dot.auction` wordmark in gold + red) and sampled its exact pixels (`#f1c62e` gold, `#d71016` red, confirmed via a `Pillow` histogram of the flattened PNG) rather than guessing — palette: `--ink #17110f`, `--ink-soft #221a17`, `--paper #f4ece0`, `--stone #c7b39c`, `--gold #f1c62e`, `--red #d71016`, single warm near-black base, no pure black. Typography: `Fraunces` (display, an auction-catalog-appropriate soft serif) + `Work Sans` (body) + `IBM Plex Mono` (data/labels) — deliberately a different pairing from Mines' `Outfit`/`Plus Jakarta Sans`/`JetBrains Mono`, no Inter. Layout: asymmetric hero (photo + gradient, left-aligned copy) over a divided list of six shipped capabilities styled as auction-catalog "Lot 01"–"Lot 06" entries (a domain-appropriate structural device — real auction catalogs number lots — replacing the old 3-card grid), plus a genuinely sequential 4-step "List → Bid → Watch → Close" lifecycle strip (kept numbered because it is an actual sequence, per the frontend-design skill's own numbering caveat). Signature element: a quiet line-art gavel-and-sound-block silhouette (echoing the real gavel icon in the mark, the way Mines echoed its own headframe icon), reused at low opacity in both the hero and the closing CTA section — no headframe reuse, no generic glow orbs. Copy rewritten from this wiki's own §1/§3/§4 and the architecture table in §2: dropped the fabricated-sounding claims and the "Live Bidding, Real Time" hype framing, kept only what's actually shipped (Reverb broadcast, reserve/buy-now pricing, seller dashboard, categories, watchlists with real live-triggered outbid notifications, multi-item lots) and explicitly did not link guests to `/auctions` — confirmed via `php artisan route:list` that `auctions.index`/`auctions.show` sit behind the `auth:sanctum`+`verified` middleware group despite the wiki calling it "buyer-facing," so guest CTAs go only to `route('register')`/`route('login')`, which are real and unauthenticated. Removed the zero `href="#"` dead links (there were none to begin with, but confirmed via grep) and the decorative `float-animation`/ping/glow-orb clutter. Motion: one restrained `IntersectionObserver` scroll-reveal plus `scale(0.97)` press-feedback, `prefers-reduced-motion` respected, no perpetual animation. Found and fixed a real, pre-existing functional bug while rebuilding: `resources/js/app.js` is empty and `alpinejs` isn't in `package.json` at all, so the guest welcome page's `x-data`/`x-show`/`@click` mobile-menu and scroll-state directives had no Alpine runtime to bind to (the authenticated layout gets Alpine from a CDN `<script>` in `layouts/app.blade.php`, which the guest page never includes) — added the identical CDN-pinned `<script defer src="https://unpkg.com/alpinejs@3.10.2/...">` tag directly to `welcome.blade.php`, matching the exact pattern already used elsewhere in this repo rather than introducing a new npm dependency. Verified end-to-end: `npm run build` clean; served locally via `php artisan serve` (with `SESSION_DRIVER=array`/`DB_CONNECTION=sqlite` overridden for this one-off local preview only, since no local Postgres instance was available and this platform's real `SESSION_DRIVER=database` needs one — `.env` itself was never touched) and rendered in a real browser at 1600px, 500px, and 375px. Confirmed via direct DOM measurement (`getBoundingClientRect`) that the nav logo renders at 80px height (64px on mobile) and the footer logo at 44px — applying the Mines `dfc4547` lesson ("small and unreadable" at 36px/28px) proactively instead of shipping it small and needing a follow-up fix. Also caught and fixed a real responsive bug found only by checking a narrow viewport: the hero's live-capability strip separators (`›`) wrapped onto their own orphaned line independently of the label they belonged to at ~500px width; regrouped each label+chevron pair into one non-breaking `inline-flex` unit. Confirmed the mobile hamburger menu actually opens (Alpine-driven, not just styled) with a fully opaque dropdown background and legible links. `public/build/` is `.gitignore`d in this repo (unlike Mines, where built assets are committed) — left as-is, matching this repo's own existing convention rather than force-adding it. |
| 0.6.1 | 2026-08-04 | Platform-loop pass | Ecosystem-wide null-`currentTeam` sweep (mirrors Dot.Mines commit `0cc4362`). Dot.Auction has no custom team-context middleware (`app/Http/Middleware` doesn't exist; `bootstrap/app.php`'s `withMiddleware` hook is empty) — team context comes straight from Jetstream's stock `HasTeams::currentTeam()`, which self-heals a null `current_team_id` by switching to `Auth::user()->personalTeam()`. That fallback is itself null for any user who owns zero teams — reachable today only for a user provisioned outside `App\Actions\Fortify\CreateNewUser` (the only code path that creates a personal team), but Jetstream's own `Team::purge()` (`vendor/laravel/jetstream/src/Team.php:110-121`) already nulls `current_team_id` for any member of a *non-personal* team that gets deleted, so the null state itself is definitely reachable — it just self-heals for users with a personal team and stays null for users without one. Confirmed the domain data (`auctions`, `bids`, `watchlists`) is scoped by `seller_id`/`bidder_id`/`user_id`, not `team_id` (see 0.6.0 entry), so `currentTeam` is used only for the Jetstream account-settings UI, not for any authorization-critical query — no `abort(403, ...)` call site existed anywhere in `app/` to match a convention against. Found and fixed 4 genuinely-unguarded occurrences, all in Blade views since no Livewire component or controller in this repo touches `currentTeam`: `resources/views/navigation-menu.blade.php:46` and `:193` called `route('teams.show', Auth::user()->currentTeam->id)` with no guard — a teamless user hitting either nav (desktop dropdown, mobile menu) would pass `null` into a required route parameter; fixed by adding `&& Auth::user()->currentTeam` to the enclosing `@if (Jetstream::hasTeamFeatures())` blocks (lines 25 and 187) so the whole team-management section is skipped for a teamless user instead of generating a broken link. `resources/views/navigation-menu.blade.php:29` (`{{ Auth::user()->currentTeam->name }}`) was inside that same now-guarded block, so it's covered by the same fix. `resources/views/layouts/app.blade.php:135` and `:148` already had a `?? 'Personal'` fallback, so they were not a crash risk (PHP emits a warning, not a `TypeError`, when reading a property off `null`) but did rely on that non-obvious PHP behavior; switched both to `Auth::user()->currentTeam?->name ?? 'Personal'` to make the null-safety explicit and remove the warning. Added a centralized guard at the primary post-login landing page (`routes/web.php`'s `/dashboard` closure — this app has no `DashboardController`/Livewire `Dashboard` component to add a `resolveCurrentTeam()` helper to) that redirects a teamless user to `route('teams.create')` (confirmed to exist via `php artisan route:list | grep -i team`) before any view renders, matching the Dot.Mines `Dashboard::mount()` pattern as closely as the routing style here allows. Regression test added to `tests/Feature/DashboardTest.php`: `test_authenticated_user_with_no_team_is_redirected_to_team_creation` creates a plain `User::factory()->create()` (every other test in the file uses `->withPersonalTeam()`) and asserts `/dashboard` redirects to `route('teams.create')`. Spot-checked the `HasUserScope` trait rollout from 0.6.0 — still intact, no gap: `Watchlist` is the only model with a `user_id`/`team_id`/`organization_id` column that is genuinely single-owner, and it already uses the trait; `Auction`/`Bid`/`AuctionCategory`/`AuctionItem` are correctly excluded (cross-tenant-visible marketplace data) per the trait's own doc comment. Full suite against a fresh isolated `dot_auction_audit_test` Postgres database (created and dropped for this pass only, real `.env` untouched, scratch `.env.testing` used and removed afterward): 67 tests, 64 passed, 3 failed, 7 skipped, 123 assertions — the 3 failures (`WatchlistTest::test_a_user_can_add_and_remove_an_auction_from_their_watchlist`, `WatchlistTest::test_scope_alone_blocks_cross_user_access_even_without_a_policy_check`, `DashboardTest::test_dashboard_shows_watchlist_count`) are a pre-existing `SQLSTATE[42703]: column "id" does not exist` on `Watchlist::create()` — the `watchlists` table has a composite primary key and no surrogate `id` column, and `Watchlist` (added in 0.3.0) never declared `public $incrementing = false`, so Eloquent's insert tries to `RETURNING "id"`. Left unfixed: it's a model/schema mismatch unrelated to the `currentTeam` audit and out of this pass's scope (no migration/model changes beyond what this sweep required); none of the 4 files this pass touched are involved. |
| 0.6.0 | 2026-08-04 | Platform-loop pass | Added `App\Models\Concerns\HasUserScope`, an Eloquent global-scope trait (the ecosystem's per-tenant-column pattern, alongside Dot.Finance's `HasUserScope` for single-user platforms and Dot.Notify's `HasTeamScope` for Jetstream-teams platforms) that restricts every query on the trait's host model to `Auth::id()` at the `user_id` column, so a forgotten `where(...)` filter can no longer leak another tenant's rows. Dot.Auction ships Jetstream Teams (`Features::teams()` is active in `config/jetstream.php`), but confirmed via the schema (`database/migrations/2026_06_27_000001_create_auction_tables.php`) that no auction-domain table carries a `team_id` column — `auctions`, `bids`, and `watchlists` are all keyed to individual `users` columns (`seller_id`/`bidder_id`/`user_id`). So this trait is the user-scoped variant, not the team-scoped one, despite teams being enabled at the platform level. Applied it to **`Watchlist` only** — a buyer's watchlist is genuinely private, with no legitimate reason for one user's query to surface another's saved-item rows. Deliberately **not** applied to `Auction`, `Bid`, or `AuctionCategory`/`AuctionItem`: these are intentionally cross-tenant-visible marketplace data (any user must be able to browse any seller's listings and see an auction's full bid history), and scoping them to a single owning user's id would break that legitimate visibility — confirmed no other private per-user model (saved searches, draft bids, etc.) exists in `app/Models/`. Removed now-redundant explicit `where('user_id', ...)` filters on `Watchlist` queries in `AuctionController@show`, `WatchlistButton` (mount/toggle), and the dashboard route (`routes/web.php`); left `Watchlist::create([...])`'s explicit `user_id` mass-assignment untouched (the scope only governs reads, not writes) and left the `WatchlistButton::toggle()` composite-primary-key delete workaround as-is. No `WatchlistPolicy` exists in this codebase, so there was no 403→404 fail-closed transition to verify — the scope is the only access control on these routes today, which is exactly the "defense in depth" gap the new regression test (`test_scope_alone_blocks_cross_user_access_even_without_a_policy_check`) targets: creates a watchlist row as one user, logs in as a different user, and asserts both `Watchlist::query()->count()` and an unfiltered `where('auction_id', ...)->exists()` see nothing, then confirms the owner still sees it (the test avoids `Model::find($id)`-by-id since `watchlists` has a composite primary key and no surrogate `id` column). Added `phpstan.neon.dist` (level 5, `larastan/larastan` extension, `paths: [app]`) and `larastan/larastan`/`phpstan/phpstan` as dev dependencies; `vendor/bin/phpstan analyse --memory-limit=1G` ran successfully in this pass (no sandbox limitation hit) and reported 15 pre-existing errors, all in Jetstream scaffolding and event/notification property access unrelated to this change — nothing in `Watchlist.php` or `HasUserScope.php`. `composer.lock` already carried a `guzzlehttp/guzzle`/`psr7`/`promises` bump from the prior pass; `composer audit` reports no security vulnerability advisories found. Full suite against a fresh isolated `dot_auction_pilot` Postgres database: 66 tests, 59 passed, 7 skipped (pre-existing, unrelated to this change), 0 failed, 133 assertions. |
| 0.5.0 | 2026-08-03 | Sakhile Bhayi | First real-execution verification pass against a live PHP/PostgreSQL toolchain (this codebase had never been run before). `composer install` succeeded on stock PHP 8.5 (no version downgrade needed). A fresh `migrate` against an isolated Postgres database ran clean, but `php artisan test` surfaced one real bug: `WatchlistTest::test_a_user_can_add_and_remove_an_auction_from_their_watchlist` failed because `WatchlistButton::toggle()` called `$watchlist->delete()` on an Eloquent model instance — but the `watchlists` table (see `2026_06_27_000001_create_auction_tables.php`) has a composite primary key (`user_id`, `auction_id`) and no surrogate `id` column, so Eloquent's instance `delete()`, which keys off `getKey()`/`id`, silently deleted zero rows. Fixed `app/Livewire/Auctions/WatchlistButton.php` to delete via the query builder scoped to both composite-key columns instead of a model-instance delete. Final test suite: 65 tests, 58 passed, 7 skipped, 0 failed (128 assertions). Applied the Dot.Brain ADR-0013 idempotent guard (`Schema::hasTable`/`hasColumn` checks) to this platform's six shared Jetstream-core migrations so they're safe to run in any order against the shared `infodot` database; re-ran migrate and the full test suite afterward on a fresh database and got the identical 65/58/7/0 result, confirming the guard is behavior-neutral. |
| 0.4.0 | 2026-08-03 | Sakhile Bhayi | `resources/views/welcome.blade.php` was still the unmodified default Laravel/Jetstream scaffold ("Let's get started", Documentation/Laracasts links) — no marketing content, no logo, no hero section existed to swap. Built a full custom marketing page from scratch, matching the structural pattern already piloted on `mines`/Dot.Mines (fixed nav + hero + features + how-it-works + platform/ecosystem + CTA + footer), with the real `public/images/logo.png` lockup in the nav and footer, and a real photographic hero background: a wooden-gavel-on-a-dark-surface photo by Sasun Bughdaryan (@sasun1990), unsplash.com/photos/wooden-gavel-resting-on-a-dark-surface-next-to-book-FaTLrG5-ViE, hotlinked via Unsplash's CDN (`images.unsplash.com/photo-1767972463877-b64ba4283cd0`) under a dark gradient overlay. Copy is drawn honestly from this wiki's own domain-entity, events, and roadmap sections (§3–§6) — real shipped features only (live Reverb bidding, reserve/buy-now pricing, seller dashboard, categories, watchlists, multi-item lots); no fabricated stats or testimonials. Verified the image URL resolves with `curl -sI` (HTTP/2 200) before committing. |
| 0.3.0 | 2026-08-01 | Auction Platform Lead | Platform-loop engineering pass: built the buyer-facing marketplace (`/auctions`, `/auctions/{auction}`) and gave `BidPanel` its missing Blade view; added `Watchlist`/`AuctionItem` Eloquent models (tables existed, no models did); added `AuctionPolicy` (draft-visibility + reserve-visibility gates) and baked reserve-price confidentiality into every buyer-facing view (`Auction::reserveMet()` only — raw `reserve_price` is never sent to a non-seller); added a watchlist toggle, a database-channel notification bell, and a real (not manually-dispatched) `OutbidNotification` fired from `BidPanel::placeBid()`; added dashboard widgets (watchlist count, ending-soon list); wired the real logo/favicons across nav, auth, and browser tab, removing unreferenced "Coming Soon" template leftovers (`index.html`, `styles.css`, stray logo PNGs/SVG); added Feature tests; brought README up to date with what's actually implemented. Auto-bid execution and the settlement job remain unbuilt — see §6. |
| 0.2.0 | 2026-08-01 | Auction Platform Lead | Rewrote wiki from actual codebase (Laravel 12 / Livewire 3 / Reverb) instead of a blueprint framing — documented shipped schema, models, and the `BidPlaced` broadcast event; cross-referenced Dot.Brain's mechanism-scoping rules for future Knowledge Pack work |
| 0.1.0 | 2026-08-01 | Auction Platform Lead | Initial placeholder wiki |

## Open Questions

- Should auto-bid execution run as a queued job per new bid, or a scheduled sweep — affects fairness guarantees under concurrent bidding.
- Where does the settlement job live: a Laravel scheduled command here, or a Dot.Billing-triggered webhook — affects who owns the "auction ended" moment of truth.
- Reserve-price confidentiality (per Dot.Brain's gate: reserves publish only as met/not-met rates, never values) needs to be enforced in the Knowledge Pack publisher once it exists, not left to convention. As of this pass, it's enforced in the buyer-facing UI (`AuctionController@show` never reads `reserve_price`, only `reserveMet()`) — the Knowledge Pack publisher itself still doesn't exist.
